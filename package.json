{
  "name": "ira-saf",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 8080",
    "format": "npx prettier --write ."
  },
  "dependencies": {
    "lit": "^2.0.2"
  },
  "devDependencies": {
    "eslint": "^8.16.0",
    "prettier": "2.6.2",
    "sass": "^1.52.1",
    "vite": "^2.9.9"
  },
  "lint-staged": {
    "**/*": "prettier --write --ignore-unknown"
  }
}
